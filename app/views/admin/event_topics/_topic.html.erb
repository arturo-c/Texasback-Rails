<%
if @event
	checked = @event.topic_ids.include?(topic.id)
else
	checked = false
end
%>

<li id="event_topic_<%= topic.id %>">
		<%= check_box_tag 'event[topic_ids][]', topic.id, checked, :id => "topic_#{topic.id}" %>
		<%= label_tag "topic_#{topic.id}", h(topic.name) %>
</li>