<%= render :partial => 'admin/utility/preview_bar', :locals => { 
																		:preview_path => "http://texasback.com#{h(@draft_version.page.url)}",
																		:button_link => edit_content_path(:slug => params[:slug]) } %>

<%= render :partial => 'conditions/sidebar' %>

<% content_for :head do %>	
	<script type="text/javascript">
	document.documentElement.className = 'js';
	</script>
<% end %>

<%= render :partial => 'conditions/index_content' %>

<%= rich_format @draft_version.body_content %>

<%= rich_format @diagnostics_page.part_content('body') %>

<ul>
	<% @diagnostics.each do |d| %>
		<li><%= link_to(h(d.name), view_diagnostic_path(:slug => d.slug)) %></li>	
	<% end %>
</ul>